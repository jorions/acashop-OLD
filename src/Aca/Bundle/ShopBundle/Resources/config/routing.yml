aca_shop_homepage:
    path:     /hello/{name}/{age}
    defaults: { _controller: AcaShopBundle:Default:index }

aca_login_route:
    path: /login_form
    #the path here is representative of the BundleFolder:ControllerName("Controller" is assumed):FunctionName("Action" is assumed)
    defaults: {_controller: AcaShopBundle:Default:loginForm }

aca_process_login_route:
    path: /process_login
    #the path here is representative of the BundleFolder:ControllerName("Controller" is assumed):FunctionName("Action" is assumed)
    defaults: {_controller: AcaShopBundle:Default:processLogin }